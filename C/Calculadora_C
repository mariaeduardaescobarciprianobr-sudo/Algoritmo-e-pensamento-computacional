#include <stdio.h> 
#include <math.h>

int main() {
    int x = 7, y = 5;
    int soma = x + y;
    int sub = x - y;
    int mult = x * y;
    float div = (float)x / y;
    int resto = x % y;
    double base = 7.0;
    double expoente = 5.0;
    double = pow(7.0, 5.0);
    double raizX = sqrt(x);
    double raizY = sqrt(y);
    double cuboX = pow(x, 3);
    double cuboY = pow(y, 3);
    int absol = abs(x - y);
    int maior = (x > y) ? x : y;
    int menor = (x < y) ? x : y;
    float media = (float)(x + y) / 2;
    float porcen = ((float)x / y) * 100;
    int quadra(X) = x * x;
    int quadra(Y) = y * y;
    double log = log(x);
    double exponen = exp(x);

    
    printf("X = %d e y = %d\n", x, y);
    printf("Soma = %d\n", soma);
    printf("Subtração = %d\n", sub);
    printf("Multiplicação = %d\n", mult);
    printf("Divisão (x/y) = %.2f\n", div);
    printf("Resto da divisão (x %% y) = %d\n", resto);
    printf("Expotencial = %,2f\n" double);
    printf("Raiz deX = %.2f\n", raizX);
    printf("Raiz deY = %.2f\n", raizY);
    printf("Cubo deX = %.2f\n", cuboX);
    printf("Cubo deY = %.2f\n", cuboY);
    printf("Valor absoluto da diferença = %d\n", absol);
    printf("Maior valor = %d\n", maior);
    printf("Menor valor = %d\n", menor);
    printf("Média = %.2f\n", media);
    printf("X em porcentagem de Y = %.2f%%\n", porcen);
    printf("Quadrado de X = %d\n", quadraX);
    printf("Quadrado de Y = %d\n", quadraY);
    printf("Logaritmo X = %.2f\n", logaritmo);
    printf("Exponencial de X = %.2f\n", exponencial);
    return 0;
